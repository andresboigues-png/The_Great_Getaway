node_modules
frontend/static/js/app.bundle.js
frontend/static/js/app.bundle.js.map
# Vite-bundled output: content-hashed JS chunks + extracted CSS. Generated.
frontend/static/js/chunks
frontend/static/js/assets
facts_dict.js
scratch
tests/__init__.py
src
package-lock.json
.claude
test-results
playwright-report

# Pre-Phase-D legacy: HTML + CSS will be reformatted when we extract
# the design system. Ignoring for now to keep CI clean.
frontend/static/css/index.css
frontend/templates/index.html
# Jinja templating syntax breaks Prettier's HTML parser — the {# #}
# block comments and {% %} control statements are non-standard.
# Server-rendered share page; doesn't ship to the client bundle.
frontend/templates/share.html
# Server-rendered Trip Templates public preview page (same Jinja reason).
frontend/templates/template.html

# Hand-written narrative docs — maintained by hand, not worth enforcing
# Prettier on (and its markdown reformatting is non-idempotent on
# ROADMAP.md, so --check would stay red even after --write). Same posture
# as the legacy CSS/HTML above.
ROADMAP.md
FUTURE_FEATURES.md
migrations/README
